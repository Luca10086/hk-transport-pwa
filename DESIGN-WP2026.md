# 森友出行 · WP × 2026「液態玻璃」皮膚設計定稿（DESIGN-WP2026.md）

> 版本：v1 定稿（2026-09-08）｜ 概念 demo：`wp2026-concept.html`（v2 華麗動效版，8 屏 + 動效實驗室）
> 規格來源：MiMo Pro 共創（材料語言 × 組件藍圖 × 華麗動效系統）＋ 主代理裁決與復審修正。
> 落地：`css/skin-wp2026.css`（`body[data-ui="wp2026"]`，預設深色；淺色雙通道已建基座）。

---

## 1. 設計原則（Metro DNA × 2026 注入）

1. **信息即界面**：Metro 大字排版、磁貼網格、Pivot/App Bar 骨架不變——2026 只改「材料與動效」，不改信息結構。
2. **液態玻璃**：所有容器 = 半透明層 + 背景模糊 + 1px 白描邊 + 頂緣反光條 + 對角反光；玻璃必須有東西可糊（背景光斑）。
3. **單一強調色**：主 `#0078D7`，輔 `#00B4D8` 僅用於高光/微光/分組小標；**禁彩虹、禁多色漸變**。
4. **華麗而克制**：動效皆有「暫停/降級」條件；內容可讀延遲 ≤50ms。
5. **可訪問性**：`prefers-reduced-motion` 全局歸零（僅功能位移 ≤0.1s）；電量省電/低幀率降級見 §7。

## 2. 設計令牌（深色主通道）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | `#05070D` | 全局背景（藍黑） |
| `--surface` | `#0A1220` | 低谷表面 |
| `--text` | `#FFFFFF` | 主文字 |
| `--text2` | `rgba(255,255,255,.55)` | 次文字/標籤 |
| `--line` | `rgba(255,255,255,.16)` | 描邊/分隔 |
| `--accent` | `#0078D7` | 主強調（可被用戶強調色覆蓋） |
| `--accent-dark` | `#005A9E` | 深強調 |
| `--aux` | `#00B4D8` | 輔助（僅高光/微光/分組小標） |
| glass 填充 | `rgba(255,255,255,.07)` | 玻璃層 |
| glass blur | `blur(26px) saturate(1.4)` | 面板/磁貼 |
| glass 描邊 | `1px rgba(255,255,255,.16)` | 所有玻璃容器 |
| 頂緣反光條 | 內凹 `inset 0 1px 0 rgba(255,255,255,.25)` | 玻璃容器頂邊 |
| 對角反光 | `linear-gradient(135deg, rgba(255,255,255,.06), transparent 70%)` | 玻璃容器 |
| 微光 | `rgba(255,255,255,.12)` / 色光 `rgba(0,120,215,.25–.45)` | 僅活躍態/軌跡 |

淺色通道：`--bg #EDF1F7`、玻璃 `rgba(255,255,255,.55)`、文字 `#16243A`、描邊 `rgba(0,0,0,.12)`、光斑透明度減半。

## 3. 字級與字重（沿用 Metro 表）

42/72/32/28/24/22/17/13；標題 Light 300、小標 600、功能數字 300 + `tabular-nums`（等寬掃視）；特大數字 42px（磁貼）/60px（溫度）/28px（ETA）。

## 4. 組件規格（落地版）

| 組件 | 規格 |
|---|---|
| 背景光斑 | 2–3 顆 150–190px 漸變圓（主/輔色極淡），18–26s 漂浮 + 4s 呼吸；夜間 opacity .35 + 降飽 8% |
| 頂欄 | 玻璃欄：`rgba(10,10,10,.6)` + `blur(48px) saturate(1.6)` + 底部 1px 高光；標題 42px Light |
| 磁貼 | 玻璃磁貼：blur 26 + 填充 .07 + 1px 描邊 + 內凹頂反光 + 14px 圓角；數字 42px Light；按壓 `scale(.96)+亮度+8%` 120ms |
| 搜索欄 | 玻璃膠囊：高 48px、`blur(40px) saturate(1.8)`、聚焦外發光 + 頂緣反光 |
| 列表行 | 玻璃行（性能守則：**行不透視模糊**，僅填充+描邊+頂反光）；72px 行高；40px 圓路線號（首行微光全強）；按壓 Y 微壓 + 左緣 3px accent 條 |
| 詳情 Sheet | 玻璃底層：`rgba(15,15,15,.72)` + `blur(60px) saturate(2)` + 頂 20px 圓角 + 手柄；去/回程 3px 色條；當前站 1.6s 脈衝光環 |
| App Bar | 玻璃欄：blur 48 + 1px 頂高光；活動態 #0078D7 + 4px 膠囊指示器（色光 10px）；圖標 1.5px 線條 |
| 設定 | 玻璃行 + 玻璃開關（滑子 .2s 彈性位移，開態藍光）；分組小標 #00B4D8 |
| K75P 實時頁 | 卡片/狀態列/站點面板玻璃化；地圖容器星空底；巴士/當前站脈衝 1.6s |

## 5. 動效系統（華麗版，含暫停條件）

| 動效 | 規格 | 暫停/降級 |
|---|---|---|
| 開場編舞 | 標題 400ms fadeInUp → 磁貼 80ms 階梯（.94→1.02 回彈）→ 搜索欄 280ms；≤800ms | 列表刷新重繪時不觸發（`.no-anim`） |
| 玻璃流光 | 45° 對角 #fff .05 掃過 **1.2s / 9s 週期** | 滾動/觸摸/實時刷新時暫停（靜置 1.5s 恢復） |
| Pivot 轉場 | 離場 blur 26→12 + 偏移 24px；入場對側匡入 320ms（.16,1,.3,1） | 手勢跟手時抑制光條 |
| 三層按壓 | 磁貼 .96+亮度 +8%／行 Y 壓+左 3px accent／按鈕圓波紋 .22s（後續迭代） | reduced-motion 歸零 |
| 光斑 | 22s 漂浮 + 4s 呼吸；懸停磁貼時向觸點 0.8s 緩移並提亮至 .12 | 滾動時 2 幀/秒；節能停 |
| 實時脈衝 | 當前站/巴士座標 1.6s 呼吸（距離 <3 站才啟動）；ETA 變化舊上淡出新下浮入 0.15/0.2s + 亮度 +4% 一閃 | 同屏脈衝 ≤2 處 |

曲線：統一 `cubic-bezier(.16,1,.3,1)`。

## 6. 情境色溫

- **夜間（21:00–06:00 / 系統深色）**：光斑 opacity .35＋降飽 8%；流光間距 12s；禁用亮度 flash。
- **極省（電量 <15%）**：停光斑/流光；脈衝改靜態。
- **節日**：主色 ±10° 色相微調；仍單色調，禁頻閃。

## 7. 降級清單

`prefers-reduced-motion` → 裝飾動效歸零（僅 ≤0.1s 功能位移）／電量 <15% → 停光斑流光／幀率 <45fps → 光斑減 1、關回彈／滾動 >2000px/s → 抑制疊層動畫。

## 8. 落地狀態與後續

| 項目 | 狀態 |
|---|---|
| 深色令牌/玻璃材料/光斑/按壓/微光/玻璃 Sheet/App Bar/設定/K75P 玻璃化 | ✅ v1 已實裝（skin-wp2026.css） |
| 玻璃流光 shimmer（磁貼 ::after 與基礎按壓層衝突） | 待 v2（改用獨立覆蓋層元素） |
| 按鈕圓波紋 ripple | 待 v2 |
| ETA 數字滾動換值 | 待 v2（接入 JS 值變更鉤子） |
| Pivot blur 視差 | 待 v2（需 JS 橫滑鉤子 + 全局玻璃底板） |
| 淺色通道完整校驗 | 待 v2（基座已建） |
| 節日情境 | 待 v3 |

參考：概念 demo `wp2026-concept.html`；審查紀錄在 `tools/reviews/`。
