{
  "name": "senyou-wp8",
  "version": "1.0.0",
  "description": "森友出行 · Windows Phone 8 風格安卓應用",
  "private": true,
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:copy": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/core": "^7.1.0",
    "@capacitor/android": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0"
  }
}
