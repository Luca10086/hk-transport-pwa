# 森友出行 · WP8 / WP10 介面設計規範與 UI/UX・可訪問性審查

版本：Beta 0.1 系列　·　適用：wp8-concept（APK 版）　·　更新：2026-08

---

## 1. 設計目標

「森友出行」以公共交通實時資訊為核心，介面致敬 Windows Phone 8（Metro）與
Windows 10（UWP/Fluent）兩個時代的設計語言。核心原則：

1. **內容優先**：信息即介面，不做裝飾性視覺噪音。
2. **扁平純色**：無陰影、無漸變；色彩只承擔「強調、分組、狀態」三種職責。
3. **大字排版**：輕字重（Light）標題 + 常規字重正文，形成清晰的層級對比。
4. **可訪問**：對比度、觸控目標、減少動畫、語音、高對比模式齊備。

---

## 2. 設計令牌（墨綠系統）

### 2.1 色彩

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | `#101D15` | 全局背景（墨綠黑） |
| `--surface` | `#1B2E22` | 卡片/App Bar 底色 |
| `--surface2` | `#24382B` | 輸入框/懸停面 |
| `--line` | `#2E4536` | 分隔線 |
| `--text` | `#FFFFFF` | 主文字（對比 ≈ 17.7:1 ✓ AAA） |
| `--text2` | `#A9BFB1` | 次文字（對比 ≈ 9.1:1 ✓ AAA） |
| `--accent` | `#2FA36B` | 主強調（翠綠，對比 ≈ 5.5:1 ✓ AA） |
| `--accent-dark` | `#1B5E20` | 深強調 |

磁貼牆（WP8 官方色板改為墨綠系純色）：
`#1B5E20` 墨綠 · `#2E7D32` 森綠 · `#00695C` 青綠 · `#558B2F` 橄欖

淺色主題：`--bg #F2F5F3`、`--surface #FFFFFF`、`--text #0E1F15`（對比 ≈ 15:1 ✓）。

### 2.2 字體

| 層級 | 字族 | 字重 | 大小 |
|---|---|---|---|
| Pivot 大標題（WP8） | Segoe UI Light | 300 | 3rem |
| Pivot 標題（WP10） | Segoe UI | 600 | 2rem |
| 分頁標題 | Segoe UI Light | 300 | 1.7rem |
| 列表主文字 | Segoe UI / Noto Sans TC | 400 | 0.95rem |
| 列表次文字 | 同上 | 400 | 0.78rem |
| 磁貼數字 | Segoe UI Light | 300 | 1.5rem |

### 2.3 間距與網格

- 邊距：手機 24px（窄屏 16px）；大屏/摺疊 32px。
- 磁貼牆：固定 4 列，間距 12px；小=1/4、中=1/2、寬=全寬。
- 觸控目標：≥ 44×44px（App Bar 按鈕 56×44、星號/刪除/語音按鈕 36px，可接受區）。
- 頂欄：高度 92px + 系統狀態欄實測高度（`--sysbar-h`），與安卓狀態欄保持 ≥20px 呼吸距離。

### 2.4 動效

- 曲線：`cubic-bezier(.16,1,.3,1)`（快出緩收）。
- 時長：列表入場 340ms（交錯 35ms）；翻頁/詳情推入 340ms；磁貼翻轉 700ms。
- 全景視差用 `requestAnimationFrame` 緩動插值。
- `prefers-reduced-motion` 全局降級為幾乎無動畫。

---

## 3. WP8（Metro）皮膚規範

| 元素 | 規範 |
|---|---|
| 導航 | Pivot 橫滑（scroll-snap）；大標題即內容 |
| 首頁 | Panorama 三面板（磁貼/搜索/天氣+K75P），背景**純色無漸變** |
| 磁貼 | 4 列牆、純色、按壓 3D Tilt、天氣磁貼翻轉、長按換色/隱藏/拖動排序 |
| App Bar | 底部裸字形（Windows 用 Segoe UI Symbol，Android 用等風格細線 SVG） |
| 列表 | 扁平行：路線號｜名稱｜副資訊｜右側大時間；Metro 分組標題 |
| 詳情 | 全屏右滑入 + 去程/回程橫滑；背景微縮「翻頁」感 |

## 4. WP10（UWP）皮膚規範

以 `body[data-ui="wp10"]` 覆蓋，與 WP8 共享全部令牌與邏輯：

| 元素 | 與 WP8 的差異 |
|---|---|
| 頂欄/App Bar | **壓克力**（半透明 + `backdrop-filter: blur(18px)`） |
| 標題 | 中型 2rem Semibold，非全小寫 |
| 磁貼 | 改為「快捷按鈕」：高度 64/56/48px、4px 圓角、600 字重 |
| 輸入框 | UWP TextBox：2px 邊框 + 淺色填充 |
| 按鈕/Chip | 2px 邊框、4px 圓角、實心強調 |
| 列表 | 行距加大至 15px，字重 400 |
| 詳情頁 | 壓克力頂欄 |

---

## 5. 可訪問性審查（WCAG 2.1 AA 對照）

| 項目 | 結果 | 措施 |
|---|---|---|
| 文字對比 | ✓ 主文字 17.7:1、次文字 9.1:1、強調 5.5:1 均達標 | 墨綠令牌已按對比驗算 |
| 觸控目標 | ✓ App Bar 56×44；✕ 星號/語音按鈕 36px 略小 | 已加 44px 點擊區邊距；下版放大到 44px |
| 焦點可見 | ✕ 鍵盤導航無明顯 focus 樣式 | 列入下版（`:focus-visible` 外框） |
| 減少動畫 | ✓ `prefers-reduced-motion` 降級 | 已實作 |
| 高對比 | ✓ 獨立「高對比」開關（純黑底、白字、2px 邊框） | 已實作 |
| 語音 | ✓ 語音播報（▶ 按鈕 + ⋯ 選單，Web Speech zh-HK） | 已實作 |
| 屏幕閱讀 | △ aria-label 已標 App Bar/搜尋框；動態列表缺少 live region | 下版加 `aria-live` |
| 文字縮放 | △ 未設 text-size-adjust | 下版驗證系統字體放大 |
| 色盲 | ✓ 狀態不只靠顏色（文字+顏色並用：即將/延誤/定時） | 已確認 |

**已知問題**：天氣磁貼長文本曾出界 → 已加 `overflow:hidden + ellipsis` 單行省略。

---

## 6. 一致性開發指引

1. 新功能一律以**共享令牌**（`--bg/--surface/--accent…`）上色，禁止硬編碼。
2. 新皮膚差異只寫進 `wp10-skin.css`（以 `body[data-ui="wp10"]` 前綴），不動基礎層。
3. 列表行沿用 `.metro-row` 構型：路線號｜名稱｜副資訊｜右側時間。
4. 動效只用 `cubic-bezier(.16,1,.3,1)`，時長 300–400ms。
5. 每次改動需過三關：WP8 深色、WP10 深色、淺色+高對比。

---

## 7. 已知限制與後續

- 桌面 Widget（#10）：npm 註冊表無可用 Capacitor Widget 插件，需原生 Android 開發，暫緩。
- 巴士實時位置僅港鐵巴士（MTR Bus）提供座標；KMB/CTB API 無位置字段。
- Android 硬件返回鍵目前直接退出，建議接入 Capacitor App 插件處理。
