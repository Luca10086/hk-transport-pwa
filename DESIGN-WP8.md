# Windows Phone 8 设计规范归纳 — 森友出行（APK 概念版）

> 参考：微软官方 [8 Tips for Designing Windows Phone Apps](https://learn.microsoft.com/zh-tw/archive/blogs/mssaudicommunity/8-tips-for-designing-windows-phone-apps)、[Tile design guidelines](https://learn.microsoft.com/zh-tw/previous-versions/windows/apps/jj662929(v=vs.105))、[Windows Phone 8 UI 设计指南（优设网译文）](https://www.uisdc.com/windows-phone-8-guideline)。

## 一、布局网格（Layout Grid）

| 规则 | 值 |
|---|---|
| 页面边距 | 左/右边距 **12px 或 24px**（内容、标题、标头一律左对齐） |
| 元素间距 | 水平与垂直方向保持一致，推荐 **12px 网格** |
| 底部留白 | 可滚动内容底部留 **95px**（避开 App Bar / 底栏） |
| 字段与数值成对 | 两组左对齐队列，或「标题在内容上方」 |

## 二、视觉语言（Metro / Modern）

1. **扁平**：无渐变、无阴影、无拟物；「内容即界面」（Content over chrome）。
2. **排版驱动**：Segoe UI / Segoe WP 字体族；标题用 Light 细体大字（34–48px），正文 12px 起；文字是首要视觉元素。
3. **单一强调色（Accent）**：全局只有一个主题强调色（系统 20 色可选）；选中态、磁贴、导航高亮统一用它。
4. **深色 / 浅色主题**：系统级黑白双主题（默认深色 #000/#1F1F1F，文字白）。

## 三、控件与交互

| 控件 | 规范 |
|---|---|
| **点击响应** | 标准控件用「**倾斜（Tilt）**」缩放动画反馈；**禁止**点击时变色 |
| **列表** | 扁平列表；点按即倾斜动画；除选择器外**无持久选中态**；用字号/颜色建立层级 |
| **图标** | 简洁单色 Metro 线条图标；**圆圈内图标=操作**，无圈=指示 |

## 四、导航结构（核心）

### Pivot（枢轴，多页面横滑）
- 至少 2 页；**水平滑动**切换页面；大标题位于左上
- 页面内**禁止**再放水平滚动控件（手势冲突）
- 空页面用占位文本提示，不移除页面

### Panorama（全景，宽幅画布）
- 最多 **5 个面板**；整页横向平移；**背景图/纹理 + 视差**（标题移动比内容慢）
- 面板内水平/垂直滚动二选一；避免浮动按钮；常规操作放 App Bar

### App Bar（底部应用栏）
- 底部条；**圆形图标按钮** = 操作；**「⋯」** 收二级操作
- 常用操作（刷新/搜索/设定）常驻；可随面板变化

### Live Tiles（动态磁贴）
- 方形（小/中）与宽形磁贴；强调色填充；可显示**动态内容**（如 ETA 数字）
- 翻转/轮播动画更新内容

### 导航原则
- 顶层导航 = Panorama/Pivot；操作 = App Bar；返回 = 系统返回键
- **没有汉堡菜单**（那是 WP10 的 NavigationView）

## 五、动效（Motion）

- 轻快、短促（0.2–0.4s）；页面切换用滑动/淡入；点按用倾斜；无弹性动画以外的花哨效果。

## 六、本概念版落实清单

| 规范 | 概念版实现 |
|---|---|
| 扁平无阴影 | 全站纯色块、1px 分隔线 |
| Segoe UI | 字体栈 `'Segoe UI Light','Segoe UI',...` |
| 单一 accent | 青色 `#00A2E8`（可换） |
| 深色主题 | 默认 `#1F1F1F` 背景 + 白字，设定可切浅色 |
| 边距/间距 | 24px 页边距、12px 间距、95px 底部留白 |
| Pivot | 4 页横滑（scroll-snap）+ 左上大标题 |
| Panorama | 首页宽幅背景 + 视差感标题 |
| App Bar | 底部 4 圆图标 + ⋯ |
| Live Tiles | 首页强调色磁贴网格（含实时 ETA 磁贴） |
| 点击反馈 | 统一「倾斜」缩放动画，无变色 |
| 列表 | 扁平行，无选中态，文字层级 |
| 动效 | 滑动/淡入切换、点按倾斜 |
